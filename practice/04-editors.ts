/**
 * New editor
 * `cmd+n`
 *
 * Close editor
 * `cmd+w`
 *
 * New window
 * `cmd+shift+n`
 */

/**
 * Split editor
 * `cmd+\`
 *
 * Move editor to other group
 * `cmd+ctrl+left/right`
 *
 * Move editor
 * `cmd+k cmd+shift+left/right`
 */

/**
 * Focus on editor group
 * `cmd+1/2/3/...`
 * `cmd+k cmd+left/right`
 */

/**
 * Focus next editor
 * `cmd+opt+left/right`
 */

/**
 * Move editor group
 * `cmd+k left/right`
 */
