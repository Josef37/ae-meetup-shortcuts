/**
 * Split editor
 * `cmd+\`
 *
 * Move editor to other group
 * `cmd+ctrl+left/right`
 *
 * Go trough editors
 * `ctrl+tab` and `ctrl+shift+tab`
 * `cmd+opt+left` and `cmd+opt+right`
 * `cmd+shift+[` and `cmd+shift+]`
 *
 * Focus on editor group
 * `cmd+1/2/3/...`
 * `cmd+k cmd+left/right`
 *
 * Move editor group
 * `cmd+k left/right`
 */
