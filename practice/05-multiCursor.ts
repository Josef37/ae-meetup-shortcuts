/**
 * Select next occurrence
 * `cmd+d`
 *
 * Undo cursor
 * `cmd+u`
 */

/**
 * Select all occurrences
 * `cmd+shift+l`
 */

/**
 * Multi-cursor
 * `opt+click`
 * `cmd+opt+up/down`
 */

/**
 * Box selection
 * `opt+shift+click`
 */

/**
 * Add cursors to line ends
 * `opt+shift+i`
 */
